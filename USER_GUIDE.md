# Scriptforge 使用指南

Scriptforge 是一款 AI 驱动的剧本编辑器，支持 Fountain 格式，内置多角色 AI 对白生成、导演模式、RAG 记忆系统和多种导出格式。

---

## 目录

- [快速开始](#快速开始)
- [界面概览](#界面概览)
- [编辑器](#编辑器)
- [AI 面板](#ai-面板)
- [文件管理](#文件管理)
- [导出](#导出)
- [键盘快捷键](#键盘快捷键)
- [AI 生成功能](#ai-生成功能)
- [导演模式](#导演模式)
- [风格模板](#风格模板)
- [桌面版特性](#桌面版特性)
- [常见问题](#常见问题)

---

## 快速开始

### 环境要求

- Python 3.12+
- Node.js 18+
- Rust（仅桌面版需要）
- Google Gemini 或 DeepSeek API Key

### 启动开发环境

**1. 启动后端**

```bash
cd backend
pip install -r requirements.txt
cd ..
uvicorn backend.main:app --reload --port 8000
```

**2. 启动前端**

```bash
cd frontend
npm install
npm run dev
```

打开浏览器访问 `http://localhost:3000`。

### 启动桌面版

```bash
cd src-tauri
cargo tauri dev
```

桌面版会自动启动后端 sidecar 服务并打开原生窗口。

### 最小使用流程

1. 在 AI 面板顶部配置栏选择 Provider，填入 API Key
2. 在「角色」面板中添加 2-3 个角色，设定人设
3. 在「场景」面板中描述场景背景
4. 切换到「AI」面板，点击「生成下一段」
5. 预览区流式显示对白，确认后点击「插入到剧本」

---

## 界面概览

```
┌─────────────────────────────────────────────────────────────┐
│  MenuBar（菜单栏）         [项目标题]                         │
├───────────────────────────────────────────┬─────────────────┤
│                                           │  LLM 配置栏      │
│                                           │  Provider | 模型  │
│           Fountain 编辑器                  │  API Key        │
│          （主编辑区域）                     ├─────────────────┤
│                                           │[角色][场景][AI]  │
│                                           │[大纲][对话][评测] │
│                                           │                 │
│                                           │  （当前面板内容） │
├───────────────────────────────────────────┴─────────────────┤
│  StatusBar（状态栏）  第 N 场 | N 页 | 已保存     模型名称    │
└─────────────────────────────────────────────────────────────┘
```

---

## 编辑器

编辑器基于 TipTap 富文本引擎，原生支持 Fountain 剧本格式。

### Fountain 元素

在编辑器中输入以下内容会自动识别为对应的剧本元素：

| 元素 | 输入方式 | 示例 |
|------|----------|------|
| 场景标题 | 以 `INT.`、`EXT.`、`INT./EXT.`、`I/E.` 开头 | `INT. 咖啡馆 - 白天` |
| 角色名 | 全大写英文或中文名（前一行为空行） | `张三` 或 `JOHN` |
| 对白 | 紧跟角色名之后的行 | `你好，很高兴认识你。` |
| 括号注释 | 用圆括号包裹 | `（低声地）` |
| 动作描述 | 普通文本，或用 `!` 强制 | `她转身离开了房间。` |
| 转场 | 全大写且以 `TO:` 结尾 | `CUT TO:` |
| 居中文本 | `>文本<` 格式 | `>第一幕<` |
| 章节标题 | `#`、`##`、`###` 开头 | `# 第一幕` |
| 分页符 | 三个或更多等号 | `===` |

**强制标记**：用 `.` 前缀强制场景标题（如 `.闪回`），用 `@` 前缀强制角色名（如 `@旁白`）。

### Tab 键循环

在编辑器中按 `Tab` 键可以循环切换当前行的元素类型：

```
普通文本 → 场景标题 → 角色名 → 对白 → 括号注释 → 动作描述 → 场景标题 ...
```

---

## AI 面板

### LLM 配置栏

配置栏固定在 AI 面板顶部，切换标签页时始终可见。

- **Provider**：Gemini / DeepSeek / 自定义（OpenAI 兼容接口）
- **模型**：Gemini 和 DeepSeek 提供下拉选项；选择「自定义」时手动填写模型名
- **API Key**：对应 Provider 的密钥
- **Base URL**：仅在「自定义」Provider 下显示，填写 OpenAI 兼容接口地址

---

右侧面板包含 6 个标签页：

### 角色管理

管理参与对话的 AI 角色。

- **添加角色**：输入角色名和性格/人设描述，点击「+ 添加角色」
- **编辑角色**：鼠标悬停角色卡片，点击 ✎ 图标进入编辑模式
- **删除角色**：鼠标悬停角色卡片，点击 ✕ 图标

角色的人设描述会直接影响 AI 生成对白时的语言风格和行为逻辑。描述越具体，生成效果越好。

**人设描述示例**：
> 王医生，45岁，性格沉稳内敛，说话简短有力，从不表露情感。有一个不为人知的秘密。

### 场景设定

设置当前场景的背景信息和风格。

- **场景描述**：自由文本，描述场景的时间、地点、氛围
- **风格模板**：可选择「默认」「悬疑」「喜剧」「现实主义」四种风格

### AI 生成控制

触发 AI 生成的操作面板。LLM 配置（Provider / 模型 / API Key）在顶部配置栏设置，此处只需选择生成方式。

- **导演模式**：勾选后启用编剧→导演→角色→审稿四阶段流水线（详见[导演模式](#导演模式)）

**生成按钮**：

| 按钮 | 功能 |
|------|------|
| 生成下一段 | 为每个角色生成一轮对白，以预览形式呈现，确认后插入编辑器 |
| 续写整场戏 | 生成完整的一场戏（Fountain 格式），自动追加到编辑器 |
| 润色选中文本 | 对选中的文本进行 AI 润色；无选中时润色最后一段 |

生成过程中可随时点击「取消」中止。

**生成预览**：点击「生成下一段」后，生成的对白会先出现在预览区，流式显示完成后提供两个操作——「插入到剧本」将对白追加到编辑器并写入 RAG 记忆，「丢弃」则直接清除。导演模式下预览区还会显示本轮的剧情目标和审稿反馈。

### 大纲导航

自动从编辑器内容中提取场景标题（`INT./EXT.`）和章节标题（`#`）生成导航列表。

- 点击条目可快速跳转到编辑器对应位置
- 场景条目以 ▸ 标识，章节条目以 # 标识

### 对话面板

与 AI 创作助手自由对话。助手自动感知当前剧本内容、角色设定、场景和风格，适合随时问问题、讨论情节、寻求修改建议。

在编辑器中选中文本后切换到对话面板，助手会将选中内容作为上下文，可直接讨论该片段。

### 评测面板

一键评测当前剧本中所有对白的质量，输出三项指标和综合评分。

| 指标 | 说明 |
|------|------|
| CPD（角色性格偏差） | 各角色语言风格的区分度，越高说明角色个性越鲜明 |
| DE（对白效率） | 信息密度与废话率的综合评分 |
| OOC（出戏率） | 由 LLM 判断角色是否说了"不像他的话" |

综合评分 0-100。评测需要至少 2 句对白，且需设置 API Key。

---

## 文件管理

### 菜单栏操作

通过左上角「文件」菜单：

| 操作 | 说明 |
|------|------|
| 新建项目 | 清空编辑器，重置标题为「未命名剧本」 |
| 导入 .fountain | 打开本地 `.fountain` 或 `.txt` 文件 |
| 导出 .fountain | 将当前内容保存为 `.fountain` 文件 |
| 导出 PDF (打印) | 生成 US Letter 规格的打印版 HTML，使用 `Cmd+P` 打印为 PDF |
| 导出 .fdx (Final Draft) | 导出为 Final Draft XML 格式 |
| 保存到服务器 | 将项目持久化到本地数据库（`backend/data/scriptforge.db`）。虽然菜单写的是「服务器」，实际上数据存在本机，不会上传到任何远程服务。这里的「服务器」指的是本地运行的后端进程 |
| 从服务器加载... | 从本地数据库中加载之前保存的项目 |

### 自动保存

编辑器每 5 秒自动将以下内容保存到浏览器 localStorage：

- 剧本内容（Fountain 文本）
- 角色列表
- 场景设定和风格
- 项目标题和作者

刷新页面或重新打开时会自动恢复上次的编辑状态。

### 主题切换

通过「视图」菜单或快捷键 `Cmd/Ctrl + D` 切换浅色/深色主题。

---

## 导出

支持三种导出格式：

### Fountain (.fountain)

纯文本格式，兼容所有 Fountain 编辑器。导出时自动添加标题页元数据（标题、作者、日期）。

### PDF（打印版）

生成符合行业标准的剧本排版 HTML：
- US Letter 纸张 (8.5" x 11")
- Courier Prime 12pt 等宽字体
- 标准剧本边距和缩进
- 自动生成标题页

在新窗口中打开后，使用 `Cmd/Ctrl + P` 打印为 PDF 文件。

### Final Draft XML (.fdx)

导出为 Final Draft 格式，可直接在 Final Draft 软件中打开编辑。

---

## 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd/Ctrl + S` | 保存 |
| `Cmd/Ctrl + E` | 导出 .fountain |
| `Cmd/Ctrl + Shift + E` | 导出 PDF |
| `Cmd/Ctrl + D` | 切换深色/浅色主题 |
| `Cmd/Ctrl + Enter` | 切换到 AI 面板 |
| `Cmd/Ctrl + 1` | 切换到「角色」面板 |
| `Cmd/Ctrl + 2` | 切换到「场景」面板 |
| `Cmd/Ctrl + 3` | 切换到「AI」面板 |
| `Cmd/Ctrl + 4` | 切换到「大纲」面板 |
| `Cmd/Ctrl + /` 或 `F1` | 显示/隐藏帮助面板 |
| `Tab`（编辑器内） | 循环切换元素类型 |

---

## AI 生成功能

### 生成下一段

为每个角色生成一轮对白。支持 SSE 流式输出，对白会逐字追加到编辑器中。

AI 生成时会综合考虑：
- 角色的人设描述
- 当前场景设定
- 已有的对话上下文
- 选择的风格模板
- RAG 记忆系统中的相关历史

### 续写整场戏

生成一场完整的戏，包含场景标题、角色对白和动作描述，以 Fountain 格式直接追加到编辑器。适合快速起草初稿。

### 润色文本

对已有文本进行 AI 润色：
- 选中文本时，润色选中部分
- 未选中时，自动润色最后一段（约 500 字符）

### 无 API Key 模式

未设置 API Key 时，系统会使用模拟数据（mock data）进行演示，方便体验界面功能。

---

## 导演模式

导演模式启用一个四阶段 AI 创作流水线，基于 CrewAI 多 Agent 框架：

```
编剧规划 → 导演分配 → 角色演出 → 审核检查
```

### 阶段详情

1. **编剧规划**：根据当前剧情阶段（开场/发展/高潮/结局），规划本轮的剧情目标、冲突和情感走向

2. **导演分配**：选择本轮发言角色，为每个角色提供具体的表演指导（态度、情绪、关系定位）

3. **角色演出**：每个被选中的角色按照导演指示和自身人设生成对白

4. **审核检查**：检查生成结果的质量，评估四个维度（各 0-10 分）：
   - 角色一致性
   - 剧情推进
   - 内容质量
   - 互动自然度

   审核不通过时会自动重试（最多 2 次）。

### 降级机制

导演模式在 CrewAI 不可用时会自动降级为直接 Gemini 调用，仍然生成剧情目标和角色对白，但不经过多 Agent 协作。

在「AI」面板中勾选「导演模式」即可启用。

---

## 风格模板

提供三种预设风格模板，影响 AI 的语言风格和创作方向：

| 模板 | 特点 | 适用场景 |
|------|------|----------|
| 悬疑 | 气氛营造、信息隐藏、伏笔铺设 | 推理、犯罪、惊悚题材 |
| 喜剧 | 轻松幽默、节奏明快 | 喜剧、轻松题材 |
| 现实主义 | 贴近生活、细节真实 | 家庭、社会、情感题材 |

每个模板包含：
- 语言特征（词汇、句式、语气）
- 示例对话（few-shot）
- 反面案例（anti-patterns）

在「场景」面板的「风格模板」下拉框中选择。系统也会根据场景描述中的关键词自动推荐合适的模板。

---

## 桌面版特性

Tauri 桌面版在 Web 版基础上提供以下增强：

- **原生文件对话框**：导入/导出使用系统文件选择器
- **窗口标题同步**：标题栏显示项目名称，未保存时显示 `*` 标记
- **后端自启动**：内嵌 Python 后端作为 sidecar 进程，无需手动启动
- **本地数据存储**：项目数据保存在系统应用数据目录中

### 构建桌面版

```bash
# 1. 构建 Python sidecar
./scripts/build_sidecar.sh

# 2. 构建 Tauri 应用
cd src-tauri
cargo tauri build
```

---

## 常见问题

### API Key 在哪里获取？

- **Gemini**：访问 [Google AI Studio](https://aistudio.google.com/) 创建 API Key
- **DeepSeek**：访问 [DeepSeek Platform](https://platform.deepseek.com/) 创建 API Key

也可以在项目根目录创建 `.env` 文件设置默认 Key：

```
GEMINI_API_KEY=your_key_here
```

### 生成失败怎么办？

1. 检查 API Key 是否正确填写
2. 确认选择的 Provider 和 Key 匹配
3. 查看状态栏的错误信息
4. 确保后端服务正在运行

### 如何获得更好的生成效果？

- 为角色写详细的人设描述（性格、说话风格、背景故事）
- 在场景描述中包含足够的背景信息
- 选择合适的风格模板
- 尝试使用导演模式获得更有结构的对白
- 使用润色功能优化初稿

### 数据保存在哪里？

| 数据 | 位置 |
|------|------|
| 自动保存 | 浏览器 localStorage |
| 项目数据库 | `backend/data/scriptforge.db`（本地 SQLite，不联网） |
| RAG 记忆 | `backend/data/chroma/`（ChromaDB） |
| 桌面版数据 | 系统应用数据目录 |
